/*import * as React from 'react';
import { Button, View, StyleSheet, Text, StatusBar,TextInput, TouchableOpacity } from 'react-native';

class Match extends React.Component {

  _ChangeVueListe(){
    this.props. navigation.navigate('ListMatch')
  }
  _ChangeVueAjout(){
    this.props.navigation.navigate('AjoutMatch')
  }
  _ChangeVueAjoutPoint(){
    this.props.navigation.navigate('AjoutPoint')
  }
  render(){

  return (
    <View style={{ flex: 1,backgroundColor: '#fff', }}>
    <View>
      <Button title="Voir la liste des matchs" onPress={() =>{this._ChangeVueListe()}} />
      <Button title="Ajouter un match"  onPress={() => {this._ChangeVueAjout()}} />
      <Button title="Ajouter des Points "  onPress={() => {this._ChangeVueAjoutPoint()}} />
    </View>
      
     
    </View>
  );
}
}

const Styles = StyleSheet.create({
  
})
export default Match

*/